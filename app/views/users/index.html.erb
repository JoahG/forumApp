<%if current_user%><% if current_user.email == "joahd@gbergfamily.com" %>
<div class="hero-unit form"><h1>Admin: All Users</h1>
<br>
<ul class="no-lst">
	<% @users.each do |user| %>
		<li><%= link_to user.name, user %>: &nbsp; <%= link_to 'Edit', edit_user_path(user) %> | <%= link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %></li>
	<%end%>
</ul></div>
<%else%>
	<%redirect_to_home("You do not have permissions to do this") %>
<% end %><%else%>
	<%redirect_to_home("You do not have permissions to do this") %><%end%>