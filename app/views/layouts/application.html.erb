<!DOCTYPE html>
<html>
<head>
  <title>Forum App</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= javascript_include_tag(:defaults) %>
  <%= javascript_include_tag :rails %>
  <%= csrf_meta_tag %>
  <script type="text/javascript">
    // to set WMD's options programatically, define a "wmd_options" object with whatever settings
    // you want to override.  Here are the defaults:
    wmd_options = {
      // format sent to the server.  Use "Markdown" to return the markdown source.
      output: "Markdown"
    };
  </script>
</head>
<body>
  <div class="navbar navbar-inverse navbar-top navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
          <div class="nav-collapse collapse">
            <nav class="left">
              <ul class="nav">
                <% if !current_page?(posts_path) && !current_page?(root_path) %>
                  <li class="">
                    <%= link_to "Home", root_url %>
                  </li>
                <% else %>
                  <li class="">
                    <a href="#">Home</a>
                  </li>
                <%end%>
                <% if current_user %>
                  <% if current_user.email == 'joahd@gbergfamily.com' %>
                    <li class="">
                      <%= link_to "Admin Page", users_path %>
                    </li>
                  <% end %>
                  <li class="">
                    <%= link_to 'New Post', new_post_path %>
                  </li>
                <%else%>
                  <li class="">
                    <%= link_to "Sign Up", sign_up_path %>
                  </li>
                <% end %>
              </ul>
            </nav>
            <!--<nav class="center">
              <!--/*<% flash.each do |name, msg| %>
                <%= content_tag :div, msg, :id => "flash_#{name}" %>
              <% end %>*/
            </nav>-->
            <nav class="right">
              <%if current_user%>
                <ul class="nav">
                  <li>
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                      <span class="arrow"></span>
                     <%= image_tag avatar_url(current_user, 16), :class => 'current_user_pic' %> &nbsp; <%= current_user.name %>
                      <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu">
                      <li>
                        <%= link_to 'View my profile', current_user %>
                      </li>
                      <li>
                        <%= link_to 'Edit my account', edit_user_path(current_user) %>
                      </li>
                      <li class="">
                        <%= link_to "Log Out", log_out_path %>
                      </li>
                    </ul>
                  </li>
                  <li>
                    <%=link_to pluralize(current_user.notifications.length, 'notification'), current_user %>
                  </li>
                </ul>
              <%else%>
                <%= form_tag sessions_path, :class => 'navbar-form pull-left' do %>
                  <%= text_field_tag :email, params[:email], :placeholder =>'Email Address' %>
                  <%= password_field_tag :password,nil, :placeholder => 'Password' %><%= submit_tag "Log in", :class => 'btn' %>
              <% end %>
              <%end%>
            </nav>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="container">
<%= yield %>
</div>
</body>
</html>
