<!DOCTYPE html>
<html>
<head>
  <title>Forum App</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tag %>
</head>
<body>
<div id="container">
<div id="user_nav">
  <% if current_user %>
    Logged in as <%= link_to current_user.email, current_user %>.
    <%= link_to "Log out", log_out_path %> or 
    <%= link_to "go home", root_url %><% if current_user.email == 'joahd@gbergfamily.com' %> or to the <%= link_to "Admin page", users_path %><%end%>
  <% else %>
    <%= link_to "Sign up", sign_up_path %> or
    <%= link_to "log in", log_in_path %> or 
    <%= link_to "go home", root_url %>
  <% end %>
  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, :id => "flash_#{name}" %>
  <% end %>
</div>

<%= yield %>
</div>
</body>
</html>
